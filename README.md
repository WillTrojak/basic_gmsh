# Basic GMSH

These are a set of basic scripts for generating test meshes in the GMSH 2.2
standard.

The scripts are largely self contained, just requiring numpy.

For more information on each script run `$ python <script> --help`.
